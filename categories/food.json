[
  {
    "id": "themeDailyMenu",
    "name": "Daily Menu",
    "type": "code",
    "requiredFields": [
      "phone",
      "address",
      "logoUri"
    ],
    "code": "const { regular, bold, script, icons } = await loadFonts();\nconst { name, phone, address, logo } = brandData;\nconst scale = getScale(width, height);\n\n// 1. Chalkboard / Dark Background Header\nconst paint = Skia.Paint();\npaint.setColor(Skia.Color('#1F1F1F'));\ncanvas.drawRect({ x: 0, y: 0, width, height: 160 * scale }, paint);\n\n// 2. Title \"TODAY'S SPECIAL\"\npaint.setColor(Skia.Color('#FFA500')); // Orange\nconst titleFont = script;\ntitleFont.setSize(70 * scale);\ndrawTextCentered(canvas, \"Today's Special\", 100 * scale, titleFont, paint, width);\n\n// 3. Floating Footer for Order Info\nconst footerH = 140 * scale;\npaint.setColor(Skia.Color('white'));\ncanvas.drawRect({ x: 0, y: height - footerH, width, height: footerH }, paint);\n\n// 4. Contact Details\npaint.setColor(Skia.Color('black'));\nconst contactFont = bold;\ncontactFont.setSize(30 * scale);\n\n// Phone (Left) with Icon\ndrawIcon(canvas, 'call', 40 * scale, height - 90 * scale, 35 * scale, paint, icons);\ndrawTextLeft(canvas, phone, 90 * scale, height - 60 * scale, contactFont, paint, width / 2 - 110 * scale);\n\n// Address (Right)\nconst addrFont = regular;\naddrFont.setSize(26 * scale);\nconst addrText = address ? address.substring(0, 25) : name;\nconst maxAddrW = width / 2 - 120 * scale;\nconst origSize = shrinkFontToFit(addrFont, addrText, maxAddrW);\nconst addrW = addrFont.getTextWidth(addrText);\ndrawIcon(canvas, 'restaurant', width - addrW - 90 * scale, height - 90 * scale, 35 * scale, paint, icons);\ncanvas.drawText(addrText, width - addrW - 40 * scale, height - 60 * scale, paint, addrFont);\naddrFont.setSize(origSize);\n\n// Logo Overlay (Top Left)\nif (logo) {\n    const lSize = 100 * scale;\n    const { width: lW, height: lH } = getLogoDimensions(logo);\n    \n  canvas.save();\n  const _clip = Skia.Path.Make();\n  _clip.addRRect(Skia.RRectXY({x: 20 * scale, y: 20 * scale, width: lSize, height: lSize * (lH/lW)}, 15 * scale, 15 * scale));\n  canvas.clipPath(_clip, 1, true);\n  drawLogo(canvas, logo, 20 * scale, 20 * scale, lSize, lSize * (lH/lW))\n  canvas.restore();\n}\n\nreturn [\"phone\", \"address\", \"logoUri\"];",
    "isPremium": false
  },
  {
    "id": "themeCoffeeBreak",
    "name": "Coffee Break",
    "type": "code",
    "requiredFields": [
      "address"
    ],
    "code": "const { condensed, bold, icons } = await loadFonts();\nconst { name, address } = brandData;\nconst scale = getScale(width, height);\n\n// Circular Badge Bottom Right\nconst circleSize = 250 * scale;\nconst cx = width - (150 * scale);\nconst cy = height - (150 * scale);\n\nconst paint = Skia.Paint();\npaint.setColor(Skia.Color('#3E2723')); // Coffee Brown\ncanvas.drawCircle(cx, cy, circleSize / 2, paint);\n\n// Inner Ring\npaint.setStyle(1); // Stroke\npaint.setStrokeWidth(5 * scale);\npaint.setColor(Skia.Color('#D7CCC8'));\ncanvas.drawCircle(cx, cy, (circleSize / 2) - (15*scale), paint);\n\n// Text inside circle\npaint.setStyle(0); // Fill\npaint.setColor(Skia.Color('white'));\nconst font = condensed;\nfont.setSize(40 * scale);\nconst w1 = font.getTextWidth(\"FRESH\");\ncanvas.drawText(\"FRESH\", cx - (w1/2), cy - (20*scale), paint, font);\n\nconst w2 = font.getTextWidth(\"DAILY\");\ncanvas.drawText(\"DAILY\", cx - (w2/2), cy + (20*scale), paint, font);\n\n// Address Bar Bottom Left\npaint.setColor(Skia.Color('white'));\nconst barW = width - (200 * scale); // Stop before circle\ncanvas.drawRect({x: 0, y: height - 100*scale, width: barW, height: 100*scale}, paint);\n\npaint.setColor(Skia.Color('#3E2723'));\nconst addrFont = bold;\naddrFont.setSize(28 * scale);\nif (address) {\n    drawIcon(canvas, 'location', 30*scale, height - 65*scale, 30*scale, paint, icons);\n    drawTextLeft(canvas, address, 70*scale, height - 40*scale, addrFont, paint, barW - 90 * scale);\n}\n\nreturn [\"address\"];",
    "isPremium": false
  },
  {
    "id": "themeChefsKiss",
    "name": "Chef's Kiss",
    "type": "code",
    "requiredFields": [
      "phone",
      "address"
    ],
    "code": "const { regular, bold, script, icons } = await loadFonts();\nconst { name, phone, address, logo } = brandData;\nconst scale = getScale(width, height);\n\nconst paint = Skia.Paint();\n\n// Bottom Gradient Overlay\nconst colors = [Skia.Color('transparent'), Skia.Color('rgba(220, 38, 38, 0.95)')]; // Red gradient\npaint.setShader(Skia.Shader.MakeLinearGradient({ x: 0, y: height - 400 * scale }, { x: 0, y: height }, colors, [0, 1], 0));\ncanvas.drawRect({ x: 0, y: height - 400 * scale, width, height: 400 * scale }, paint);\npaint.setShader(null);\n\n// Brand Name in beautiful script\npaint.setColor(Skia.Color('white'));\nconst titleFont = script;\ntitleFont.setSize(80 * scale);\ndrawTextCentered(canvas, name, height - 160 * scale, titleFont, paint, width, width - 80 * scale);\n\n// Divider Line\npaint.setColor(Skia.Color('rgba(255, 255, 255, 0.5)'));\ncanvas.drawRect({ x: width * 0.2, y: height - 130 * scale, width: width * 0.6, height: 2 * scale }, paint);\n\n// Info Text\npaint.setColor(Skia.Color('white'));\nconst infoFont = regular;\ninfoFont.setSize(24 * scale);\nconst infoText = `${phone}  \u2022  ${address}`;\ndrawTextCentered(canvas, infoText, height - 80 * scale, infoFont, paint, width, width - 80 * scale);\n\n// Circular \"Delicious\" Badge (Top Right)\nconst cx = width - 120 * scale;\nconst cy = 120 * scale;\nconst r = 80 * scale;\n\npaint.setColor(Skia.Color('#FBBF24')); // Amber\ncanvas.drawCircle(cx, cy, r, paint);\n\npaint.setStyle(1); // Stroke\npaint.setStrokeWidth(3 * scale);\npaint.setColor(Skia.Color('white'));\ncanvas.drawCircle(cx, cy, r - 10 * scale, paint);\n\npaint.setStyle(0); // Fill\nconst badgeFont = bold;\nbadgeFont.setSize(22 * scale);\n\nconst w1 = badgeFont.getTextWidth(\"100%\");\ncanvas.drawText(\"100%\", cx - (w1/2), cy - 10 * scale, paint, badgeFont);\nconst w2 = badgeFont.getTextWidth(\"FRESH\");\ncanvas.drawText(\"FRESH\", cx - (w2/2), cy + 20 * scale, paint, badgeFont);\n\nreturn [\"phone\", \"address\"];",
    "isPremium": false
  },
  {
    "id": "themeModernEats",
    "name": "Modern Eats",
    "type": "code",
    "requiredFields": [
      "socialHandle",
      "slogan",
      "logoUri"
    ],
    "code": "const { pacifico, montserrat, icons } = await loadFonts();\nconst { name, slogan, socialHandle, logo } = brandData;\nconst scale = getScale(width, height);\n\nconst paint = Skia.Paint();\n\n// 1. Bottom Glassmorphism Bar\nconst barH = 220 * scale;\nconst barY = height - barH - 40 * scale;\n\npaint.setColor(Skia.Color('rgba(255, 255, 255, 0.85)'));\ncanvas.drawRRect(Skia.RRectXY({ x: 30 * scale, y: barY, width: width - 60 * scale, height: barH }, 40 * scale, 40 * scale), paint);\n\n// 2. Circular Clipped Logo (Overlapping the bar)\nif (logo) {\n  const lSize = 140 * scale;\n  const lX = width / 2 - lSize / 2;\n  const lY = barY - (lSize / 2) - 20 * scale;\n  \n  // Draw white background ring for logo\n  paint.setColor(Skia.Color('white'));\n  canvas.drawCircle(lX + lSize / 2, lY + lSize / 2, (lSize / 2) + 8 * scale, paint);\n\n  canvas.save();\n  const clipPath = Skia.Path.Make();\n  clipPath.addCircle(lX + lSize / 2, lY + lSize / 2, lSize / 2);\n  canvas.clipPath(clipPath, 1, true);\n  \n  const { width: lW, height: lH } = getLogoDimensions(logo);\n  const s = Math.max(lSize / lW, lSize / lH);\n  const drawW = lW * s;\n  const drawH = lH * s;\n  const drawX = lX - (drawW - lSize) / 2;\n  const drawY = lY - (drawH - lSize) / 2;\n  \n  drawLogo(canvas, logo, drawX, drawY, drawW, drawH);\n  canvas.restore();\n}\n\n// 3. Brand Name in beautiful Pacifico script\npaint.setColor(Skia.Color('#F97316')); // Orange 500\nconst titleFont = pacifico;\ntitleFont.setSize(55 * scale);\ndrawTextCentered(canvas, name, barY + 80 * scale, titleFont, paint, width, width - 100 * scale);\n\n// 4. Slogan in Montserrat\npaint.setColor(Skia.Color('#3F3F46')); // Zinc 700\nconst sloganFont = montserrat;\nsloganFont.setSize(22 * scale);\nif (slogan) drawTextCentered(canvas, slogan.toUpperCase(), barY + 125 * scale, sloganFont, paint, width, width - 100 * scale);\n\n// 5. Social Handle Pill\nif (socialHandle) {\n  paint.setColor(Skia.Color('#FFF7ED')); // Orange 50\n  const pillW = 240 * scale;\n  const pillH = 50 * scale;\n  const pillX = width / 2 - pillW / 2;\n  const pillY = barY + 150 * scale;\n  canvas.drawRRect(Skia.RRectXY({ x: pillX, y: pillY, width: pillW, height: pillH }, 25 * scale, 25 * scale), paint);\n\n  paint.setColor(Skia.Color('#EA580C')); // Orange 600\n  const socialFont = montserrat;\n  socialFont.setSize(18 * scale);\n  \n  drawIcon(canvas, 'logo-instagram', pillX + 25 * scale, pillY + 13 * scale, 24 * scale, paint, icons);\n  drawTextCentered(canvas, socialHandle, pillY + 34 * scale, socialFont, paint, width, pillW - 60 * scale);\n}\n\nreturn [\"socialHandle\", \"slogan\", \"logoUri\"];",
    "isPremium": false
  },
  {
    "id": "themeFreshPlates",
    "name": "Fresh Plates",
    "type": "code",
    "requiredFields": [
      "website",
      "address",
      "logoUri"
    ],
    "code": "const { montserrat, caveat, oswald, icons } = await loadFonts();\nconst { name, address, website, logo } = brandData;\nconst scale = getScale(width, height);\n\nconst paint = Skia.Paint();\n\n// 1. Organic Shape Background (Top)\npaint.setColor(Skia.Color('#10B981')); // Emerald 500\ncanvas.drawCircle(0, 0, width * 0.6, paint);\n\npaint.setColor(Skia.Color('#047857')); // Emerald 700\ncanvas.drawCircle(width * 0.1, -width * 0.1, width * 0.5, paint);\n\n// 2. Handwritten \"Fresh\" Tag\npaint.setColor(Skia.Color('#FEF08A')); // Yellow 200\nconst freshFont = caveat;\nfreshFont.setSize(60 * scale);\n\ncanvas.save();\ncanvas.rotate(-15, 80 * scale, 100 * scale);\ncanvas.drawText(\"Always Fresh!\", 40 * scale, 120 * scale, paint, freshFont);\ncanvas.restore();\n\n// 3. Logo with rounded rectangle clip (Top Right)\nif (logo) {\n  const lSize = 140 * scale;\n  const lX = width - lSize - 40 * scale;\n  const lY = 60 * scale;\n\n  paint.setColor(Skia.Color('rgba(0,0,0,0.2)'));\n  canvas.drawRRect(Skia.RRectXY({x: lX + 5*scale, y: lY + 5*scale, width: lSize, height: lSize}, 30*scale, 30*scale), paint);\n  \n  paint.setColor(Skia.Color('white'));\n  canvas.drawRRect(Skia.RRectXY({x: lX, y: lY, width: lSize, height: lSize}, 30*scale, 30*scale), paint);\n\n  canvas.save();\n  const clipPath = Skia.Path.Make();\n  clipPath.addRRect(Skia.RRectXY({x: lX, y: lY, width: lSize, height: lSize}, 30*scale, 30*scale));\n  canvas.clipPath(clipPath, 1, true);\n  \n  const { width: lW, height: lH } = getLogoDimensions(logo);\n  const s = Math.max(lSize / lW, lSize / lH);\n  const drawW = lW * s;\n  const drawH = lH * s;\n  drawLogo(canvas, logo, lX - (drawW - lSize) / 2, lY - (drawH - lSize) / 2, drawW, drawH);\n  canvas.restore();\n}\n\n// 4. Dark Bottom Panel\nconst panelH = 260 * scale;\npaint.setColor(Skia.Color('#18181B')); // Zinc 900\ncanvas.drawRect({ x: 0, y: height - panelH, width, height: panelH }, paint);\n\n// 5. Brand Name in Oswald\npaint.setColor(Skia.Color('white'));\nconst nameFont = oswald;\nnameFont.setSize(55 * scale);\ndrawTextLeft(canvas, name.toUpperCase(), 40 * scale, height - panelH + 70 * scale, nameFont, paint, width - 80 * scale);\n\n// 6. Address & Website\npaint.setColor(Skia.Color('#A1A1AA')); // Zinc 400\nconst infoFont = montserrat;\ninfoFont.setSize(22 * scale);\n\nif (address) {\n  drawIcon(canvas, 'location', 40 * scale, height - 130 * scale, 28 * scale, paint, icons);\n  drawTextLeft(canvas, address, 85 * scale, height - 105 * scale, infoFont, paint, width - 125 * scale);\n}\n\nif (website) {\n  paint.setColor(Skia.Color('#10B981')); // Emerald 500\n  drawIcon(canvas, 'globe', 40 * scale, height - 70 * scale, 28 * scale, paint, icons);\n  drawTextLeft(canvas, website, 85 * scale, height - 45 * scale, infoFont, paint, width - 125 * scale);\n}\n\nreturn [\"website\", \"address\", \"logoUri\"];",
    "isPremium": false
  },
  {
    "id": "themeTasteBuds",
    "name": "Taste Buds",
    "type": "code",
    "requiredFields": [
      "phone",
      "socialHandle",
      "logoUri"
    ],
    "code": "const { righteous, roboto, pacifico, icons } = await loadFonts();\nconst { name, phone, socialHandle, logo } = brandData;\nconst scale = getScale(width, height);\n\nconst paint = Skia.Paint();\n\n// Dark moody gradient overlay\nconst colors = [Skia.Color('transparent'), Skia.Color('rgba(15, 23, 42, 0.95)')];\npaint.setShader(Skia.Shader.MakeLinearGradient({ x: 0, y: height - 500 * scale }, { x: 0, y: height }, colors, [0, 1], 0));\ncanvas.drawRect({ x: 0, y: 0, width, height }, paint);\npaint.setShader(null);\n\n// Big Bold Brand Name\npaint.setColor(Skia.Color('#FBBF24')); // Amber 400\nconst nameFont = righteous;\nnameFont.setSize(65 * scale);\ndrawTextCentered(canvas, name.toUpperCase(), height - 320 * scale, nameFont, paint, width, width - 80 * scale);\n\n// Script \"Delicious\" Subtitle\npaint.setColor(Skia.Color('white'));\nconst subFont = pacifico;\nsubFont.setSize(35 * scale);\ndrawTextCentered(canvas, \"Taste the difference\", height - 260 * scale, subFont, paint, width, width - 80 * scale);\n\n// Logo in a Circle\nif (logo) {\n  const lSize = 160 * scale;\n  const lX = width / 2 - lSize / 2;\n  const lY = height - 560 * scale;\n  \n  // Outer Ring\n  paint.setColor(Skia.Color('#FBBF24'));\n  canvas.drawCircle(lX + lSize / 2, lY + lSize / 2, lSize / 2 + 6 * scale, paint);\n  \n  canvas.save();\n  const clipPath = Skia.Path.Make();\n  clipPath.addCircle(lX + lSize / 2, lY + lSize / 2, lSize / 2);\n  canvas.clipPath(clipPath, 1, true);\n  \n  const { width: lW, height: lH } = getLogoDimensions(logo);\n  const s = Math.max(lSize / lW, lSize / lH);\n  const drawW = lW * s;\n  const drawH = lH * s;\n  drawLogo(canvas, logo, lX - (drawW - lSize) / 2, lY - (drawH - lSize) / 2, drawW, drawH);\n  canvas.restore();\n}\n\n// Two columns for Phone & Social\nconst infoFont = roboto;\ninfoFont.setSize(24 * scale);\n\n// Left: Phone\nif (phone) {\n    paint.setColor(Skia.Color('white'));\n    const maxColW = width / 2 - 80 * scale;\n    const origSize = shrinkFontToFit(infoFont, phone, maxColW);\n    const textW = infoFont.getTextWidth(phone);\n    const startX = width / 4 - textW / 2 + 15 * scale;\n    drawIcon(canvas, 'call', startX - 35 * scale, height - 120 * scale, 28 * scale, paint, icons);\n    canvas.drawText(phone, startX, height - 95 * scale, paint, infoFont);\n    infoFont.setSize(origSize);\n}\n\n// Right: Social\nif (socialHandle) {\n    paint.setColor(Skia.Color('#FBBF24'));\n    const maxColW = width / 2 - 80 * scale;\n    const origSize = shrinkFontToFit(infoFont, socialHandle, maxColW);\n    const textW = infoFont.getTextWidth(socialHandle);\n    const startX = (width * 3) / 4 - textW / 2 + 15 * scale;\n    drawIcon(canvas, 'logo-instagram', startX - 35 * scale, height - 120 * scale, 28 * scale, paint, icons);\n    canvas.drawText(socialHandle, startX, height - 95 * scale, paint, infoFont);\n    infoFont.setSize(origSize);\n}\n\nreturn [\"phone\", \"socialHandle\", \"logoUri\"];",
    "isPremium": false
  },
  {
    "id": "themeLuxuryRestaurant",
    "name": "Luxury Restaurant",
    "type": "code",
    "requiredFields": [
      "phone",
      "address",
      "logoUri",
      "slogan"
    ],
    "code": "const { montserrat, pacifico, bold, icons } = await loadFonts();\nconst { name, phone, address, slogan, logo } = brandData;\nconst scale = getScale(width, height);\nconst paint = Skia.Paint();\n\nconst colors = [Skia.Color('rgba(0,0,0,0)'), Skia.Color('rgba(0,0,0,0.92)')];\npaint.setShader(Skia.Shader.MakeLinearGradient({x:0,y:height-500*scale},{x:0,y:height},colors,[0,1],0));\ncanvas.drawRect({x:0,y:0,width,height},paint);\npaint.setShader(null);\n\npaint.setColor(Skia.Color('#D4AF37'));\nconst titleFont = bold;\ntitleFont.setSize(70*scale);\ndrawTextCentered(canvas,name.toUpperCase(),height-280*scale,titleFont,paint,width,width-80*scale);\n\npaint.setColor(Skia.Color('white'));\nconst sloganFont = pacifico;\nsloganFont.setSize(35*scale);\ndrawTextCentered(canvas,slogan || 'Fine Dining Experience',height-220*scale,sloganFont,paint,width,width-80*scale);\n\npaint.setColor(Skia.Color('#D4AF37'));\ncanvas.drawRect({x:width*0.25,y:height-180*scale,width:width*0.5,height:3*scale},paint);\n\nconst infoFont = montserrat;\ninfoFont.setSize(24*scale);\npaint.setColor(Skia.Color('white'));\nconst infoText = phone + ' \u2022 ' + address;\ndrawTextCentered(canvas,infoText,height-120*scale,infoFont,paint,width,width-80*scale);\n\nif(logo){\nconst size=140*scale;\nconst x=width/2-size/2;\nconst y=60*scale;\npaint.setColor(Skia.Color('#D4AF37'));\ncanvas.drawCircle(x+size/2,y+size/2,size/2+8*scale,paint);\ncanvas.save();\nconst clip=Skia.Path.Make();\nclip.addCircle(x+size/2,y+size/2,size/2);\ncanvas.clipPath(clip,1,true);\nconst {width:lW,height:lH}=getLogoDimensions(logo);\nconst s=Math.max(size/lW,size/lH);\ndrawLogo(canvas,logo,x-(lW*s-size)/2,y-(lH*s-size)/2,lW*s,lH*s);\ncanvas.restore();\n}\n\nreturn ['phone','address','logoUri','slogan'];",
    "isPremium": false
  },
  {
    "id": "themeStreetFoodExpress",
    "name": "Street Food Express",
    "type": "code",
    "requiredFields": [
      "phone",
      "website",
      "logoUri"
    ],
    "code": "const { oswald, montserrat, bold, icons } = await loadFonts();\nconst { name, phone, website, logo } = brandData;\nconst scale = getScale(width, height);\nconst paint = Skia.Paint();\n\n// ===== HOT RED/ORANGE OVERLAY =====\nconst colors = [\n  Skia.Color('rgba(0,0,0,0)'),\n  Skia.Color('rgba(234,88,12,0.35)'),\n  Skia.Color('rgba(127,29,29,0.95)')\n];\n\npaint.setShader(\n  Skia.Shader.MakeLinearGradient(\n    { x: 0, y: height * 0.3 },\n    { x: 0, y: height },\n    colors,\n    [0, 0.45, 1],\n    0\n  )\n);\ncanvas.drawRect({ x: 0, y: 0, width, height }, paint);\npaint.setShader(null);\n\n// ===== SALE TAG =====\npaint.setColor(Skia.Color('#FACC15'));\ncanvas.drawRRect(\n  Skia.RRectXY(\n    {\n      x: 35 * scale,\n      y: 40 * scale,\n      width: 170 * scale,\n      height: 65 * scale\n    },\n    16 * scale,\n    16 * scale\n  ),\n  paint\n);\n\npaint.setColor(Skia.Color('#7F1D1D'));\nconst saleFont = bold;\nsaleFont.setSize(30 * scale);\ncanvas.drawText(\n  'HOT DEAL',\n  58 * scale,\n  83 * scale,\n  paint,\n  saleFont\n);\n\n// ===== LOGO =====\nif (logo) {\n  const size = 120 * scale;\n  const x = width - size - 40 * scale;\n  const y = 40 * scale;\n\n  paint.setColor(Skia.Color('white'));\n  canvas.drawCircle(\n    x + size / 2,\n    y + size / 2,\n    size / 2 + 8 * scale,\n    paint\n  );\n\n  canvas.save();\n  const clip = Skia.Path.Make();\n  clip.addCircle(\n    x + size / 2,\n    y + size / 2,\n    size / 2\n  );\n  canvas.clipPath(clip, 1, true);\n\n  const { width: lW, height: lH } = getLogoDimensions(logo);\n  const s = Math.max(size / lW, size / lH);\n\n  drawLogo(\n    canvas,\n    logo,\n    x - ((lW * s) - size) / 2,\n    y - ((lH * s) - size) / 2,\n    lW * s,\n    lH * s\n  );\n\n  canvas.restore();\n}\n\n// ===== MAIN BRAND =====\npaint.setColor(Skia.Color('white'));\nconst titleFont = oswald;\ntitleFont.setSize(90 * scale);\ndrawTextCentered(\n  canvas,\n  name.toUpperCase(),\n  height - 310 * scale,\n  titleFont,\n  paint,\n  width,\n  width - 80 * scale\n);\n\n// ===== ORDER NOW STRIP =====\npaint.setColor(Skia.Color('#FACC15'));\ncanvas.drawRRect(\n  Skia.RRectXY(\n    {\n      x: width * 0.25,\n      y: height - 250 * scale,\n      width: width * 0.5,\n      height: 55 * scale\n    },\n    30 * scale,\n    30 * scale\n  ),\n  paint\n);\n\npaint.setColor(Skia.Color('#7F1D1D'));\nconst orderFont = bold;\norderFont.setSize(24 * scale);\ndrawTextCentered(\n  canvas,\n  'ORDER NOW',\n  height - 213 * scale,\n  orderFont,\n  paint,\n  width,\n  width * 0.4\n);\n\n// ===== DELIVERY GLASS PANEL =====\npaint.setColor(Skia.Color('rgba(255,255,255,0.12)'));\ncanvas.drawRRect(\n  Skia.RRectXY(\n    {\n      x: 30 * scale,\n      y: height - 170 * scale,\n      width: width - 60 * scale,\n      height: 120 * scale\n    },\n    25 * scale,\n    25 * scale\n  ),\n  paint\n);\n\n// ===== PHONE =====\npaint.setColor(Skia.Color('#FACC15'));\ndrawIcon(\n  canvas,\n  'call',\n  60 * scale,\n  height - 125 * scale,\n  28 * scale,\n  paint,\n  icons\n);\n\nconst infoFont = montserrat;\ninfoFont.setSize(22 * scale);\n\npaint.setColor(Skia.Color('white'));\ndrawTextLeft(canvas, phone, 105 * scale, height - 102 * scale, infoFont, paint, width - 165 * scale);\n\n// ===== WEBSITE =====\npaint.setColor(Skia.Color('#FACC15'));\ndrawIcon(\n  canvas,\n  'globe',\n  60 * scale,\n  height - 72 * scale,\n  28 * scale,\n  paint,\n  icons\n);\n\npaint.setColor(Skia.Color('white'));\ndrawTextLeft(canvas, website || '', 105 * scale, height - 48 * scale, infoFont, paint, width - 165 * scale);\n\n// ===== EXPRESS DELIVERY BADGE =====\npaint.setColor(Skia.Color('#F97316'));\ncanvas.drawCircle(\n  width - 90 * scale,\n  height - 220 * scale,\n  60 * scale,\n  paint\n);\n\npaint.setColor(Skia.Color('white'));\nconst badgeFont = montserrat;\nbadgeFont.setSize(18 * scale);\n\nconst fastW = badgeFont.getTextWidth('FAST');\ncanvas.drawText('FAST', (width - 90 * scale) - (fastW / 2), height - 232 * scale, paint, badgeFont);\n\nconst deliveryW = badgeFont.getTextWidth('DELIVERY');\ncanvas.drawText('DELIVERY', (width - 90 * scale) - (deliveryW / 2), height - 202 * scale, paint, badgeFont);\n\nreturn ['phone','website','logoUri'];",
    "isPremium": false
  },
  {
    "id": "themeFreshOrganic",
    "name": "Fresh Organic",
    "type": "code",
    "requiredFields": [
      "website",
      "socialHandle",
      "logoUri",
      "slogan"
    ],
    "code": "const { montserrat, caveat, oswald, icons } = await loadFonts();\nconst { name, slogan, website, socialHandle, logo } = brandData;\nconst scale = getScale(width, height);\nconst paint = Skia.Paint();\n\n// ===== ORGANIC TOP SHAPES =====\npaint.setColor(Skia.Color('#22C55E'));\ncanvas.drawCircle(-100 * scale, -50 * scale, width * 0.55, paint);\n\npaint.setColor(Skia.Color('#15803D'));\ncanvas.drawCircle(width * 0.15, -80 * scale, width * 0.42, paint);\n\npaint.setColor(Skia.Color('#4ADE80'));\ncanvas.drawCircle(width + 80 * scale, 120 * scale, width * 0.25, paint);\n\n// ===== FRESH LABEL =====\npaint.setColor(Skia.Color('#FEF08A'));\nconst freshFont = caveat;\nfreshFont.setSize(70 * scale);\n\ncanvas.save();\ncanvas.rotate(-12, 120 * scale, 120 * scale);\ncanvas.drawText(\n  '100% Fresh',\n  40 * scale,\n  130 * scale,\n  paint,\n  freshFont\n);\ncanvas.restore();\n\n// ===== LOGO CARD =====\nif (logo) {\n  const size = 150 * scale;\n  const x = width - size - 35 * scale;\n  const y = 55 * scale;\n\n  paint.setColor(Skia.Color('rgba(0,0,0,0.15)'));\n  canvas.drawRRect(\n    Skia.RRectXY(\n      {\n        x: x + 6 * scale,\n        y: y + 6 * scale,\n        width: size,\n        height: size\n      },\n      30 * scale,\n      30 * scale\n    ),\n    paint\n  );\n\n  paint.setColor(Skia.Color('white'));\n  canvas.drawRRect(\n    Skia.RRectXY(\n      {\n        x,\n        y,\n        width: size,\n        height: size\n      },\n      30 * scale,\n      30 * scale\n    ),\n    paint\n  );\n\n  canvas.save();\n  const clip = Skia.Path.Make();\n  clip.addRRect(\n    Skia.RRectXY(\n      {\n        x,\n        y,\n        width: size,\n        height: size\n      },\n      30 * scale,\n      30 * scale\n    )\n  );\n  canvas.clipPath(clip, 1, true);\n\n  const { width: lW, height: lH } = getLogoDimensions(logo);\n  const s = Math.max(size / lW, size / lH);\n\n  drawLogo(\n    canvas,\n    logo,\n    x - ((lW * s) - size) / 2,\n    y - ((lH * s) - size) / 2,\n    lW * s,\n    lH * s\n  );\n\n  canvas.restore();\n}\n\n// ===== GLASS PANEL =====\npaint.setColor(Skia.Color('rgba(255,255,255,0.88)'));\ncanvas.drawRRect(\n  Skia.RRectXY(\n    {\n      x: 35 * scale,\n      y: height - 320 * scale,\n      width: width - 70 * scale,\n      height: 260 * scale\n    },\n    40 * scale,\n    40 * scale\n  ),\n  paint\n);\n\n// ===== BRAND =====\npaint.setColor(Skia.Color('#166534'));\nconst titleFont = oswald;\ntitleFont.setSize(60 * scale);\ndrawTextCentered(\n  canvas,\n  name.toUpperCase(),\n  height - 240 * scale,\n  titleFont,\n  paint,\n  width,\n  width - 100 * scale\n);\n\n// ===== SLOGAN =====\npaint.setColor(Skia.Color('#374151'));\nconst sloganFont = montserrat;\nsloganFont.setSize(22 * scale);\ndrawTextCentered(\n  canvas,\n  slogan || 'Healthy Food \u2022 Healthy Life',\n  height - 190 * scale,\n  sloganFont,\n  paint,\n  width,\n  width - 100 * scale\n);\n\n// ===== GREEN DIVIDER =====\npaint.setColor(Skia.Color('#22C55E'));\ncanvas.drawRect(\n  {\n    x: width * 0.3,\n    y: height - 160 * scale,\n    width: width * 0.4,\n    height: 4 * scale\n  },\n  paint\n);\n\n// ===== WEBSITE =====\npaint.setColor(Skia.Color('#16A34A'));\ndrawIcon(\n  canvas,\n  'globe',\n  70 * scale,\n  height - 120 * scale,\n  28 * scale,\n  paint,\n  icons\n);\n\nconst infoFont = montserrat;\ninfoFont.setSize(20 * scale);\n\ndrawTextLeft(canvas, website || '', 110 * scale, height - 95 * scale, infoFont, paint, width - 270 * scale);\n\n// ===== SOCIAL =====\npaint.setColor(Skia.Color('#16A34A'));\ndrawIcon(\n  canvas,\n  'logo-instagram',\n  70 * scale,\n  height - 70 * scale,\n  28 * scale,\n  paint,\n  icons\n);\n\ndrawTextLeft(canvas, socialHandle || '', 110 * scale, height - 45 * scale, infoFont, paint, width - 270 * scale);\n\n// ===== ORGANIC BADGE =====\npaint.setColor(Skia.Color('#22C55E'));\ncanvas.drawCircle(\n  width - 95 * scale,\n  height - 130 * scale,\n  55 * scale,\n  paint\n);\n\npaint.setColor(Skia.Color('white'));\nconst badgeFont = montserrat;\nbadgeFont.setSize(16 * scale);\n\nconst orgW = badgeFont.getTextWidth('ORGANIC');\ncanvas.drawText('ORGANIC', (width - 95 * scale) - (orgW / 2), height - 135 * scale, paint, badgeFont);\n\nconst foodW = badgeFont.getTextWidth('FOOD');\ncanvas.drawText('FOOD', (width - 95 * scale) - (foodW / 2), height - 108 * scale, paint, badgeFont);\n\nreturn ['website','socialHandle','logoUri','slogan'];",
    "isPremium": false
  },
  {
    "id": "themeRetroDiner",
    "name": "Retro Diner",
    "type": "code",
    "requiredFields": [
      "phone",
      "website",
      "address"
    ],
    "code": "const { righteous, bold, icons } = await loadFonts();\nconst { name, phone, website, address } = brandData;\nconst scale = getScale(width, height);\nconst paint = Skia.Paint();\n\n// 1. Neon glow border\npaint.setStyle(1);\npaint.setStrokeWidth(16 * scale);\npaint.setColor(Skia.Color('rgba(6, 182, 212, 0.25)')); // Cyan glow\ncanvas.drawRect({ x: 25 * scale, y: 25 * scale, width: width - 50 * scale, height: height - 50 * scale }, paint);\n\npaint.setStrokeWidth(6 * scale);\npaint.setColor(Skia.Color('#06B6D4')); // Cyan line\ncanvas.drawRect({ x: 25 * scale, y: 25 * scale, width: width - 50 * scale, height: height - 50 * scale }, paint);\npaint.setStyle(0);\n\n// 2. Hanging Top Signboard (Semi-translucent red)\npaint.setColor(Skia.Color('rgba(239, 68, 68, 0.9)'));\ncanvas.drawRect({ x: width * 0.2, y: 25 * scale, width: width * 0.6, height: 85 * scale }, paint);\n\n// White border on hanging sign\npaint.setStyle(1);\npaint.setStrokeWidth(2 * scale);\npaint.setColor(Skia.Color('white'));\ncanvas.drawRect({ x: width * 0.2 + 5 * scale, y: 30 * scale, width: width * 0.6 - 10 * scale, height: 75 * scale }, paint);\npaint.setStyle(0);\n\npaint.setColor(Skia.Color('white'));\nconst topFont = bold;\ntopFont.setSize(30 * scale);\ndrawTextCentered(canvas, \"OPEN 24 HOURS\", 78 * scale, topFont, paint, width, width * 0.5);\n\n// 3. Central Brand Name (Righteous, neon-style overlay with semi-translucent dark backplate for readability)\npaint.setColor(Skia.Color('rgba(0, 0, 0, 0.4)'));\ncanvas.drawRRect(\n  Skia.RRectXY(\n    {\n      x: width * 0.1,\n      y: height / 2 - 95 * scale,\n      width: width * 0.8,\n      height: 190 * scale\n    },\n    20 * scale,\n    20 * scale\n  ),\n  paint\n);\n\npaint.setColor(Skia.Color('#EF4444')); // Red shadow\nconst titleFont = righteous;\ntitleFont.setSize(90 * scale);\ndrawTextCentered(canvas, name.toUpperCase(), height / 2 + 10 * scale, titleFont, paint, width, width * 0.7);\n\npaint.setColor(Skia.Color('white')); // Main text offset\ndrawTextCentered(canvas, name.toUpperCase(), height / 2 + 7 * scale, titleFont, paint, width, width * 0.7);\n\n// Slogan/Subtext\npaint.setColor(Skia.Color('#06B6D4'));\nconst subFont = bold;\nsubFont.setSize(28 * scale);\ndrawTextCentered(canvas, \"CLASSIC AMERICAN DINER\", height / 2 + 65 * scale, subFont, paint, width, width * 0.7);\n\n// 4. Bottom Signboard for Info (semi-translucent retro red)\npaint.setColor(Skia.Color('rgba(239, 68, 68, 0.9)'));\ncanvas.drawRect({ x: 25 * scale, y: height - 130 * scale, width: width - 50 * scale, height: 105 * scale }, paint);\n\nconst infoFont = bold;\ninfoFont.setSize(25 * scale);\npaint.setColor(Skia.Color('white'));\n\n// Left info: Phone\nif (phone) {\n  drawIcon(canvas, 'call', 50 * scale, height - 92 * scale, 28 * scale, paint, icons);\n  drawTextLeft(canvas, phone, 95 * scale, height - 68 * scale, infoFont, paint, width/2 - 110 * scale);\n}\n\n// Right info: Website\nif (website) {\n  const maxW = width/2 - 110 * scale;\n  const origSize = shrinkFontToFit(infoFont, website, maxW);\n  const webW = infoFont.getTextWidth(website);\n  const startX = width - 50 * scale - webW;\n  drawIcon(canvas, 'globe', startX - 45 * scale, height - 92 * scale, 28 * scale, paint, icons);\n  canvas.drawText(website, startX, height - 68 * scale, paint, infoFont);\n  infoFont.setSize(origSize);\n}\n\nreturn [\"phone\", \"website\", \"address\"];",
    "isPremium": false
  },
  {
    "id": "themeHealthyFresh",
    "name": "Healthy & Fresh",
    "type": "code",
    "requiredFields": [
      "website",
      "socialHandle",
      "logoUri",
      "slogan"
    ],
    "code": "const { dancingScript, montserrat, lora, icons } = await loadFonts();\nconst { name, slogan, website, socialHandle, logo } = brandData;\nconst scale = getScale(width, height);\nconst paint = Skia.Paint();\n\n// 1. Leafy accent shapes in corners (leaving center transparent)\npaint.setColor(Skia.Color('rgba(16, 185, 129, 0.2)')); // Emerald translucent\ncanvas.drawCircle(-50 * scale, -50 * scale, 220 * scale, paint);\ncanvas.drawCircle(width + 50 * scale, 120 * scale, 180 * scale, paint);\n\n// 2. Handwritten Tag\npaint.setColor(Skia.Color('#059669'));\nconst tagFont = dancingScript;\ntagFont.setSize(60 * scale);\ndrawTextCentered(canvas, \"Eat Clean \u2022 Live Well\", 100 * scale, tagFont, paint, width, width * 0.5);\n\n// 3. Logo inside a floating circular badge (Top Right)\nif (logo) {\n  const size = 130 * scale;\n  const x = width - size - 40 * scale;\n  const y = 40 * scale;\n\n  // Background white ring\n  paint.setColor(Skia.Color('white'));\n  canvas.drawCircle(x + size / 2, y + size / 2, size / 2 + 8 * scale, paint);\n  \n  // Outer green ring\n  paint.setStyle(1);\n  paint.setStrokeWidth(3 * scale);\n  paint.setColor(Skia.Color('#10B981'));\n  canvas.drawCircle(x + size / 2, y + size / 2, size / 2 + 12 * scale, paint);\n  paint.setStyle(0);\n\n  canvas.save();\n  const clip = Skia.Path.Make();\n  clip.addCircle(x + size / 2, y + size / 2, size / 2);\n  canvas.clipPath(clip, 1, true);\n\n  const { width: lW, height: lH } = getLogoDimensions(logo);\n  const s = Math.max(size / lW, size / lH);\n  drawLogo(canvas, logo, x - ((lW * s) - size) / 2, y - ((lH * s) - size) / 2, lW * s, lH * s);\n  canvas.restore();\n}\n\n// 4. Forest green translucent bottom panel\nconst panelH = 300 * scale;\npaint.setColor(Skia.Color('rgba(6, 78, 59, 0.9)')); // Semi-translucent forest green\ncanvas.drawRRect(\n  Skia.RRectXY(\n    {\n      x: 30 * scale,\n      y: height - panelH - 30 * scale,\n      width: width - 60 * scale,\n      height: panelH\n    },\n    45 * scale,\n    45 * scale\n  ),\n  paint\n);\n\n// 5. Brand Name in Lora (elegant serif)\npaint.setColor(Skia.Color('#A7F3D0')); // Mint green 200\nconst titleFont = lora;\ntitleFont.setSize(72 * scale);\ndrawTextCentered(canvas, name, height - panelH + 50 * scale, titleFont, paint, width, width - 100 * scale);\n\n// 6. Slogan in Montserrat\npaint.setColor(Skia.Color('#F3F4F6'));\nconst sloganFont = montserrat;\nsloganFont.setSize(28 * scale);\nif (slogan) drawTextCentered(canvas, slogan.toUpperCase(), height - panelH + 110 * scale, sloganFont, paint, width, width - 100 * scale);\n\n// Divider line\npaint.setColor(Skia.Color('rgba(167, 243, 208, 0.3)'));\ncanvas.drawRect({ x: width * 0.25, y: height - panelH + 140 * scale, width: width * 0.5, height: 2 * scale }, paint);\n\n// 7. Info: Website and Social\npaint.setColor(Skia.Color('white'));\nconst infoFont = montserrat;\ninfoFont.setSize(24 * scale);\n\nif (website) {\n  drawIcon(canvas, 'globe', 70 * scale, height - 150 * scale, 30 * scale, paint, icons);\n  drawTextLeft(canvas, website, 115 * scale, height - 124 * scale, infoFont, paint, width - 165 * scale);\n}\n\nif (socialHandle) {\n  drawIcon(canvas, 'logo-instagram', 70 * scale, height - 90 * scale, 30 * scale, paint, icons);\n  drawTextLeft(canvas, socialHandle, 115 * scale, height - 64 * scale, infoFont, paint, width - 165 * scale);\n}\n\nreturn [\"website\", \"socialHandle\", \"logoUri\", \"slogan\"];",
    "isPremium": false
  },
  {
    "id": "themeSweetTreats",
    "name": "Sweet Treats",
    "type": "code",
    "requiredFields": [
      "phone",
      "website",
      "logoUri"
    ],
    "code": "const { pacifico, caveat, montserrat, icons } = await loadFonts();\nconst { name, phone, website, logo } = brandData;\nconst scale = getScale(width, height);\nconst paint = Skia.Paint();\n\n// 1. Soft accent details in top corners (leaving main center transparent)\npaint.setColor(Skia.Color('rgba(244, 114, 182, 0.15)')); // Soft pink\ncanvas.drawCircle(0, 0, 160 * scale, paint);\ncanvas.drawCircle(width, 0, 160 * scale, paint);\n\n// 2. Sweet Scalloped/Wavy Bottom Panel in Pink 400 (semi-translucent so background is slightly visible, or rich solid overlay)\nconst panelH = 340 * scale;\nconst panelY = height - panelH;\n\npaint.setColor(Skia.Color('rgba(244, 114, 182, 0.95)')); // Semi-translucent pink\ncanvas.drawRect({ x: 0, y: panelY, width, height: panelH }, paint);\n\n// Draw decorative circles to make a scalloped border at the top of the panel\nconst numScallops = 10;\nconst scallopRadius = width / numScallops / 2;\nfor (let i = 0; i <= numScallops; i++) {\n  canvas.drawCircle(i * scallopRadius * 2, panelY, scallopRadius, paint);\n}\n\n// 3. Logo in a decorative pink frame (positioned in the upper part of the transparent area)\nif (logo) {\n  const size = 150 * scale;\n  const x = width / 2 - size / 2;\n  const y = 60 * scale;\n\n  // Background white ring\n  paint.setColor(Skia.Color('white'));\n  canvas.drawCircle(x + size / 2, y + size / 2, size / 2 + 8 * scale, paint);\n\n  // Outer pink ring\n  paint.setStyle(1);\n  paint.setStrokeWidth(3 * scale);\n  paint.setColor(Skia.Color('#DB2777'));\n  canvas.drawCircle(x + size / 2, y + size / 2, size / 2 + 12 * scale, paint);\n  paint.setStyle(0);\n\n  canvas.save();\n  const clip = Skia.Path.Make();\n  clip.addCircle(x + size / 2, y + size / 2, size / 2);\n  canvas.clipPath(clip, 1, true);\n\n  const { width: lW, height: lH } = getLogoDimensions(logo);\n  const s = Math.max(size / lW, size / lH);\n  drawLogo(canvas, logo, x - ((lW * s) - size) / 2, y - ((lH * s) - size) / 2, lW * s, lH * s);\n  canvas.restore();\n}\n\n// 4. Brand Name in beautiful script inside the bottom panel\npaint.setColor(Skia.Color('white'));\nconst titleFont = pacifico;\ntitleFont.setSize(76 * scale);\ndrawTextCentered(canvas, name, panelY + 110 * scale, titleFont, paint, width, width - 80 * scale);\n\n// 5. Cute Annotation\npaint.setColor(Skia.Color('#FFF1F2'));\nconst annotFont = caveat;\nannotFont.setSize(44 * scale);\ndrawTextCentered(canvas, \"freshly baked with love daily\", panelY + 165 * scale, annotFont, paint, width, width - 80 * scale);\n\n// 6. Cute white capsule for contact details\npaint.setColor(Skia.Color('rgba(255, 255, 255, 0.9)'));\ncanvas.drawRRect(\n  Skia.RRectXY(\n    {\n      x: 30 * scale,\n      y: panelY + 195 * scale,\n      width: width - 60 * scale,\n      height: 110 * scale\n    },\n    25 * scale,\n    25 * scale\n  ),\n  paint\n);\n\npaint.setColor(Skia.Color('#DB2777')); // Pink 600\nconst infoFont = montserrat;\ninfoFont.setSize(24 * scale);\n\nif (phone) {\n  drawIcon(canvas, 'call', 60 * scale, panelY + 230 * scale, 28 * scale, paint, icons);\n  drawTextLeft(canvas, phone, 100 * scale, panelY + 254 * scale, infoFont, paint, width / 2 - 110 * scale);\n}\n\nif (website) {\n  const maxW = width/2 - 110 * scale;\n  const origSize = shrinkFontToFit(infoFont, website, maxW);\n  const webW = infoFont.getTextWidth(website);\n  const startX = width - 60 * scale - webW;\n  drawIcon(canvas, 'globe', startX - 45 * scale, panelY + 230 * scale, 28 * scale, paint, icons);\n  drawTextLeft(canvas, website, startX, panelY + 254 * scale, infoFont, paint, width / 2 - 110 * scale);\n  infoFont.setSize(origSize);\n}\n\nreturn [\"phone\", \"website\", \"logoUri\"];",
    "isPremium": false
  },
  {
    "id": "themeBBQGrill",
    "name": "BBQ & Grill",
    "type": "code",
    "requiredFields": [
      "phone",
      "address",
      "slogan"
    ],
    "code": "const { righteous, oswald, montserrat, icons } = await loadFonts();\nconst { name, slogan, phone, address } = brandData;\nconst scale = getScale(width, height);\nconst paint = Skia.Paint();\n\n// 1. Fiery linear gradient overlay at the bottom\nconst overlayColors = [Skia.Color('transparent'), Skia.Color('rgba(28, 25, 23, 0.95)')];\npaint.setShader(Skia.Shader.MakeLinearGradient({ x: 0, y: height - 380 * scale }, { x: 0, y: height }, overlayColors, [0, 1], 0));\ncanvas.drawRect({ x: 0, y: height - 380 * scale, width, height: 380 * scale }, paint);\npaint.setShader(null);\n\n// 2. Fiery Orange Gradient Border\nconst borderColors = [Skia.Color('#EA580C'), Skia.Color('#DC2626')];\npaint.setStyle(1);\npaint.setStrokeWidth(10 * scale);\npaint.setShader(Skia.Shader.MakeLinearGradient({ x: 0, y: 0 }, { x: width, y: height }, borderColors, [0, 1], 0));\ncanvas.drawRect({ x: 20 * scale, y: 20 * scale, width: width - 40 * scale, height: height - 40 * scale }, paint);\npaint.setShader(null);\npaint.setStyle(0);\n\n// 3. Top Banner \"SMOKED DAILY\"\npaint.setColor(Skia.Color('#DC2626'));\ncanvas.drawRect({ x: width / 2 - 165 * scale, y: 40 * scale, width: 330 * scale, height: 65 * scale }, paint);\n\npaint.setColor(Skia.Color('white'));\nconst bannerFont = oswald;\nbannerFont.setSize(32 * scale);\ndrawTextCentered(canvas, \"SMOKED DAILY\", 84 * scale, bannerFont, paint, width, 300 * scale);\n\n// 4. Central Brand Name (overlay with semi-transparent charcoal backplate for readability)\npaint.setColor(Skia.Color('rgba(28, 25, 23, 0.65)'));\ncanvas.drawRRect(\n  Skia.RRectXY(\n    {\n      x: 40 * scale,\n      y: height / 2 - 80 * scale,\n      width: width - 80 * scale,\n      height: 160 * scale\n    },\n    20 * scale,\n    20 * scale\n  ),\n  paint\n);\n\npaint.setColor(Skia.Color('#F97316')); // Orange 500\nconst titleFont = righteous;\ntitleFont.setSize(85 * scale);\ndrawTextCentered(canvas, name.toUpperCase(), height / 2 + 15 * scale, titleFont, paint, width, width - 120 * scale);\n\n// 5. Slogan (inside bottom area)\npaint.setColor(Skia.Color('#F5F5F4')); // Stone 100\nconst sloganFont = montserrat;\nsloganFont.setSize(28 * scale);\nif (slogan) drawTextCentered(canvas, slogan, height - 315 * scale, sloganFont, paint, width, width - 120 * scale);\n\n// Gold/Flame Divider\npaint.setColor(Skia.Color('#EA580C'));\ncanvas.drawRect({ x: width * 0.35, y: height - 265 * scale, width: width * 0.3, height: 3 * scale }, paint);\n\n// 6. Bottom info container (translucent charcoal)\npaint.setColor(Skia.Color('rgba(255, 255, 255, 0.08)'));\ncanvas.drawRRect(\n  Skia.RRectXY(\n    {\n      x: 40 * scale,\n      y: height - 220 * scale,\n      width: width - 80 * scale,\n      height: 150 * scale\n    },\n    20 * scale,\n    20 * scale\n  ),\n  paint\n);\n\nconst infoFont = montserrat;\ninfoFont.setSize(26 * scale);\n\n// Phone\npaint.setColor(Skia.Color('#EA580C'));\ndrawIcon(canvas, 'call', 70 * scale, height - 192 * scale, 32 * scale, paint, icons);\npaint.setColor(Skia.Color('white'));\ndrawTextLeft(canvas, phone, 115 * scale, height - 164 * scale, infoFont, paint, width - 175 * scale);\n\n// Address\npaint.setColor(Skia.Color('#EA580C'));\ndrawIcon(canvas, 'location', 70 * scale, height - 142 * scale, 32 * scale, paint, icons);\npaint.setColor(Skia.Color('white'));\ndrawTextLeft(canvas, address || '', 115 * scale, height - 114 * scale, infoFont, paint, width - 175 * scale);\n\nreturn [\"phone\", \"address\", \"slogan\"];",
    "isPremium": false
  }
]