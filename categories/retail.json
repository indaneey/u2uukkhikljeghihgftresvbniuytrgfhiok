[
  {
    "id": "themeRetail",
    "name": "Retail",
    "type": "code",
    "requiredFields": [
      "name",
      "phone"
    ],
    "code": "const {regular, bold, condensed} = await loadFonts();\n  const {name, phone} = brandData;\n  const scale = getScale(width, height);\n  const footerHeight = 140 * scale;\n  \n  // Black Bar\n  const paint = Skia.Paint();\n  paint.setColor(Skia.Color('black'));\n  canvas.drawRect({ x: 0, y: height - footerHeight, width, height: footerHeight }, paint);\n\n  \n  // Yellow Line Top of Bar\n  paint.setColor(Skia.Color('#FFD700')); // Gold\n  canvas.drawRect({ x: 0, y: height - footerHeight, width, height: 10 * scale }, paint);\n\n  const titleFont = condensed\n  titleFont.setSize(60 * scale)\n  const regularFont = regular\n  regularFont.setSize(35 * scale)\n\n  // Text White\n  paint.setColor(Skia.Color('white'));\n  drawTextCentered(canvas, name.toUpperCase(), height - (70 * scale), titleFont, paint, width);\n  \n  // Phone smaller below\n  paint.setColor(Skia.Color('#CCCCCC'));\n  drawTextCentered(canvas, phone, height - (30 * scale), regularFont, paint, width);\n\n  //RETURN REQUIRED BRAND INFO\n  return [\"name\", \"phone\"];",
    "isPremium": false
  },
  {
    "id": "themeBoldRetail",
    "name": "Bold Retail",
    "type": "code",
    "requiredFields": [
      "name",
      "address",
      "phone"
    ],
    "code": "const { bold, regular, icons } = await loadFonts();\n  const { name, phone, address } = brandData;\n  const scale = getScale(width, height);\n\n  const paint = Skia.Paint();\n  \n  // 1. Bottom Dark Gradient\n  const colors = [Skia.Color('transparent'), Skia.Color('black')];\n  paint.setShader(Skia.Shader.MakeLinearGradient({ x: 0, y: height - 500*scale }, { x: 0, y: height }, colors, [0, 1], 0));\n  canvas.drawRect({ x: 0, y: height - 500*scale, width, height: 500*scale }, paint);\n  paint.setShader(null);\n\n  // 2. Large Brand Name\n  paint.setColor(Skia.Color('white'));\n  const titleFont = bold;\n  titleFont.setSize(80 * scale);\n  drawTextLeft(canvas, name.toUpperCase(), 40 * scale, height - 220 * scale, titleFont, paint, width - (40 * scale));\n\n  // 3. Address with Icon\n  const addrFont = regular;\n  addrFont.setSize(34 * scale);\n  paint.setColor(Skia.Color('#FFD700')); // Gold\n  \n  if (address) {\n    // Icon: Location\n    drawIcon(canvas, 'location', 35 * scale, height - 160 * scale, 36 * scale, paint, icons);\n    drawTextLeft(canvas, address, 85 * scale, height - 128 * scale, addrFont, paint, width - (40 * scale));\n  }\n\n  // 4. WhatsApp Call-to-Action Bar\n  const barH = 100 * scale;\n  const barY = height - 100 * scale;\n  paint.setColor(Skia.Color('#25D366')); // WhatsApp Green\n  const rrect = Skia.RRectXY({x: 30*scale, y: barY, width: width - 60*scale, height: barH}, 20*scale, 20*scale);\n  canvas.drawRRect(rrect, paint);\n  \n  // Icon: WhatsApp Logo inside bar\n  paint.setColor(Skia.Color('white'));\n  drawIcon(canvas, 'logo-whatsapp', 60 * scale, barY + (20*scale), 55 * scale, paint, icons);\n\n  const phoneFont = bold;\n  phoneFont.setSize(50 * scale);\n  drawTextLeft(canvas, `ORDER: ${phone}`, 140 * scale, barY + (68 * scale), phoneFont, paint, width - (40 * scale));\n\n  return [\"name\", \"address\", \"phone\"];",
    "isPremium": false
  },
  {
    "id": "themeModernShop",
    "name": "Modern Shop",
    "type": "code",
    "requiredFields": [
      "name",
      "address",
      "phone"
    ],
    "code": "const { condensed, regular, icons } = await loadFonts();\n  const { name, address, phone } = brandData;\n  const scale = getScale(width, height);\n\n  // 1. Shop Name (Giant)\n  const paint = Skia.Paint();\n  paint.setColor(Skia.Color('white'));\n  // Shadow for text\n  const shadow = Skia.Paint();\n  shadow.setColor(Skia.Color('black'));\n  shadow.setAlphaf(0.5);\n\n  const nFont = condensed;\n  nFont.setSize(110 * scale); // Huge\n  \n  // Draw Shadow then Text\n  const titleY = height - 250 * scale;\n  drawTextCentered(canvas, name.toUpperCase(), titleY + 4, nFont, shadow, width);\n  drawTextCentered(canvas, name.toUpperCase(), titleY, nFont, paint, width);\n\n  // 2. Info Footer Bar\n  const barH = 140 * scale;\n  paint.setColor(Skia.Color('rgba(0,0,0,0.85)'));\n  canvas.drawRect({ x: 0, y: height - barH, width, height: barH }, paint);\n\n  paint.setColor(Skia.Color('white'));\n  const infoFont = regular;\n  infoFont.setSize(30 * scale);\n  \n  // Left Side: Address with PIN icon\n  if (address) {\n     drawIcon(canvas, 'location', 40 * scale, height - 90 * scale, 40 * scale, paint, icons);\n     // Truncate address if needed or just draw\n     canvas.drawText(address.substring(0, 20), 90 * scale, height - 55 * scale, paint, infoFont);\n  }\n\n  // Right Side: Phone with CALL icon\n  // Calculate width to align right\n  const phText = phone;\n  const phWidth = infoFont.getTextWidth(phText);\n  const phX = width - phWidth - 40 * scale;\n  \n  // Icon: Call\n  drawIcon(canvas, 'call', phX - 50 * scale, height - 90 * scale, 40 * scale, paint, icons);\n  drawTextLeft(canvas, phText, phX, height - 55 * scale, infoFont, paint, width - (40 * scale));\n\n  return [\"name\", \"address\", \"phone\"];",
    "isPremium": false
  },
  {
    "id": "themeEComm",
    "name": "E Comm",
    "type": "code",
    "requiredFields": [
      "logoUri",
      "website"
    ],
    "code": "const { regular, bold, icons } = await loadFonts();\n  const { name, logo, website } = brandData;\n  const scale = getScale(width, height);\n\n  // 1. Subtle Glass Header\n  const paint = Skia.Paint();\n  paint.setColor(Skia.Color('white'));\n  paint.setAlphaf(0.8);\n  canvas.drawRect({ x: 0, y: 0, width, height: 120 * scale }, paint);\n\n  // 2. Logo (Top Left)\n  if (logo) {\n    const logoSize = 80 * scale;\n    const { width: lW, height: lH } = getLogoDimensions(logo);\n    \n    canvas.save();\n    const _clip = Skia.Path.Make();\n    _clip.addRRect(Skia.RRectXY({x: 30 * scale, y: 20 * scale, width: logoSize, height: logoSize * (lH / lW)}, 15 * scale, 15 * scale));\n    canvas.clipPath(_clip, 1, true);\n    drawLogo(canvas, logo, 30 * scale, 20 * scale, logoSize, logoSize * (lH / lW))\n    canvas.restore();;\n  }\n\n  // 3. Price/Action Badge (Top Right)\n  paint.setColor(Skia.Color('#000000'));\n  const badgeW = 200 * scale;\n  const badgeH = 60 * scale;\n  canvas.drawRRect(Skia.RRectXY({x: width - badgeW - 30*scale, y: 30*scale, width: badgeW, height: badgeH}, 30*scale, 30*scale), paint);\n  \n  paint.setColor(Skia.Color('white'));\n  const font = bold;\n  font.setSize(24 * scale);\n  canvas.drawText(\"SHOP NOW\", width - badgeW + 20*scale, 70*scale, paint, font);\n\n  // 4. Footer URL\n  paint.setColor(Skia.Color('#555555'));\n  const urlFont = regular;\n  urlFont.setSize(28 * scale);\n  if (website) drawTextCentered(canvas, website.toLowerCase(), height - 40*scale, urlFont, paint, width);\n\n  return [\"logoUri\", \"website\"];",
    "isPremium": false
  },
  {
    "id": "themeCatalog",
    "name": "Catalog",
    "type": "code",
    "requiredFields": [
      "name",
      "phone",
      "website"
    ],
    "code": "const { regular, icons } = await loadFonts();\n  const { name, phone, website } = brandData;\n  const scale = getScale(width, height);\n\n  // 1. Semi-transparent dark footer\n  const paint = Skia.Paint();\n  paint.setColor(Skia.Color('rgba(0,0,0,0.7)'));\n  const footerH = 160 * scale;\n  canvas.drawRect({ x: 0, y: height - footerH, width, height: footerH }, paint);\n\n  // 2. Contact Grid (Two Columns)\n  paint.setColor(Skia.Color('white'));\n  const font = regular;\n  font.setSize(28 * scale);\n  \n  // WhatsApp Row\n  drawIcon(canvas, 'logo-whatsapp', 50 * scale, height - 120 * scale, 30 * scale, paint, icons);\n  drawTextLeft(canvas, phone, 100 * scale, height - 95 * scale, font, paint, width - (40 * scale));\n\n  // Website Row (if exists)\n  if (website) {\n    drawIcon(canvas, 'globe-outline', 50 * scale, height - 60 * scale, 30 * scale, paint, icons);\n    drawTextLeft(canvas, website.replace('https://', ''), 100 * scale, height - 35 * scale, font, paint, width - (40 * scale));\n  }\n\n  // Brand Name (Right Aligned)\n  const nameFont = regular;\n  nameFont.setSize(32 * scale);\n  const nameW = nameFont.getTextWidth(name);\n  drawTextLeft(canvas, name, width - nameW - 50 * scale, height - 85 * scale, nameFont, paint, width - (40 * scale));\n\n  return [\"name\", \"phone\", \"website\"];",
    "isPremium": false
  },
  {
    "id": "themeStoreLocator",
    "name": "Store Locator",
    "type": "code",
    "requiredFields": [
      "name",
      "address"
    ],
    "code": "const { regular, bold, icons, condensed } = await loadFonts();\n  const { name, address, website } = brandData;\n  const scale = getScale(width, height);\n\n  // 1. Location Bar (Bottom)\n  const barH = 180 * scale;\n  const paint = Skia.Paint();\n  paint.setColor(Skia.Color('white'));\n  canvas.drawRect({ x: 0, y: height - barH, width, height: barH }, paint);\n\n  // 2. \"Find Us\" Accent\n  paint.setColor(Skia.Color('#EF4444')); // Location Red\n  const accentH = 40 * scale;\n  canvas.drawRect({ x: 0, y: height - barH, width: 120 * scale, height: accentH }, paint);\n  \n  paint.setColor(Skia.Color('white'));\n  const findFont = bold;\n  findFont.setSize(18 * scale);\n  canvas.drawText(\"VISIT US\", 20 * scale, height - barH + (28 * scale), paint, findFont);\n\n  // 3. Address Text\n  paint.setColor(Skia.Color('#1F2937'));\n  const addrFont = regular;\n  addrFont.setSize(30 * scale);\n  \n  if (address) {\n    drawIcon(canvas, 'location', 40 * scale, height - 100 * scale, 35 * scale, paint, icons);\n    // Draw address with wrapping logic ideally, but here as single line\n    drawTextLeft(canvas, address, 90 * scale, height - 72 * scale, addrFont, paint, width - (40 * scale));\n  }\n\n  // 4. Shop Name (Right Aligned)\n  const shopFont = condensed;\n  shopFont.setSize(50 * scale);\n  const nameW = shopFont.getTextWidth(name.toUpperCase());\n  drawTextLeft(canvas, name.toUpperCase(), width - nameW - 40 * scale, height - 90 * scale, shopFont, paint, width - (40 * scale));\n\n  return [\"name\", \"address\"];",
    "isPremium": false
  },
  {
    "id": "themeProductDrop",
    "name": "Product Drop",
    "type": "code",
    "requiredFields": [
      "logoUri"
    ],
    "code": "const { regular, bold } = await loadFonts();\n  const { name, logo } = brandData;\n  const scale = getScale(width, height);\n\n  // \"NEW ARRIVAL\" Badge floating center bottom\n  const paint = Skia.Paint();\n  paint.setColor(Skia.Color('white'));\n  \n  const badgeW = 400 * scale;\n  const badgeH = 100 * scale;\n  const badgeX = (width - badgeW) / 2;\n  const badgeY = height - 150 * scale;\n  \n  canvas.drawRRect(Skia.RRectXY({x: badgeX, y: badgeY, width: badgeW, height: badgeH}, 50*scale, 50*scale), paint);\n\n  // Text\n  paint.setColor(Skia.Color('black'));\n  const font = bold;\n  font.setSize(32 * scale);\n  const text = \"NEW ARRIVAL\";\n  const tw = font.getTextWidth(text);\n  canvas.drawText(text, badgeX + (badgeW - tw)/2, badgeY + 60*scale, paint, font);\n\n  // Logo top center\n  if (logo) {\n      const lSize = 120 * scale;\n      const { width: lW, height: lH } = getLogoDimensions(logo);\n      \n    canvas.save();\n    const _clip = Skia.Path.Make();\n    _clip.addRRect(Skia.RRectXY({x: (width - lSize)/2, y: 50*scale, width: lSize, height: lSize * (lH/lW)}, 15 * scale, 15 * scale));\n    canvas.clipPath(_clip, 1, true);\n    drawLogo(canvas, logo, (width - lSize)/2, 50*scale, lSize, lSize * (lH/lW))\n    canvas.restore();;\n  }\n\n  return [\"logoUri\"];",
    "isPremium": false
  },
  {
    "id": "themeGridShowcase",
    "name": "Grid Showcase",
    "type": "code",
    "requiredFields": [
      "logoUri",
      "name"
    ],
    "code": "const { bold, regular } = await loadFonts();\n    const { name, logo } = brandData;\n    const scale = getScale(width, height);\n\n    // Draw white grid lines over photo to simulate collage\n    const paint = Skia.Paint();\n    paint.setColor(Skia.Color('white'));\n    paint.setStrokeWidth(10 * scale);\n    paint.setStyle(1); // Stroke\n    \n    // Cross\n    canvas.drawLine(width/2, 0, width/2, height, paint);\n    canvas.drawLine(0, height/2, width, height/2, paint);\n\n    // Center Badge\n    paint.setStyle(0); // Fill\n    canvas.drawCircle(width/2, height/2, 100*scale, paint);\n\n    // Logo in center\n    if (logo) {\n        const lSize = 120 * scale;\n        const { width: lW, height: lH } = getLogoDimensions(logo);\n        \n    canvas.save();\n    const _clip = Skia.Path.Make();\n    _clip.addRRect(Skia.RRectXY({x: (width-lSize)/2, y: (height-lSize)/2, width: lSize, height: lSize * (lH/lW)}, 15 * scale, 15 * scale));\n    canvas.clipPath(_clip, 1, true);\n    drawLogo(canvas, logo, (width-lSize)/2, (height-lSize)/2, lSize, lSize * (lH/lW))\n    canvas.restore();;\n    }\n\n    // Name at bottom\n    paint.setColor(Skia.Color('black'));\n    const f = bold;\n    f.setSize(40 * scale);\n    drawTextCentered(canvas, name.toUpperCase(), height - 50*scale, f, paint, width);\n\n    return [\"logoUri\", \"name\"];",
    "isPremium": false
  },
  {
    "id": "themeRetailGrandDisplay",
    "name": "Grand Display",
    "type": "code",
    "requiredFields": [
      "name",
      "website"
    ],
    "code": "const { oswald, regular } = await loadFonts();\nconst { name, website, slogan } = brandData;\nconst scale = getScale(width, height);\nconst paint = Skia.Paint();\n\n// 1. Asymmetric multi-stop protection gradient block on the right half of the image\nconst gradientPaint = Skia.Paint();\nconst colors = [Skia.Color('transparent'), Skia.Color('rgba(10, 10, 12, 0.4)'), Skia.Color('rgba(10, 10, 12, 0.95)')];\ngradientPaint.setShader(Skia.Shader.MakeLinearGradient(\n  { x: width * 0.4, y: 0 },\n  { x: width, y: 0 },\n  colors,\n  [0, 0.35, 1],\n  0\n));\ncanvas.drawRect({ x: width * 0.4, y: 0, width: width * 0.6, height }, gradientPaint);\n\n// 2. Vertical architectural accent layout (Champagne Gold marker line)\npaint.setColor(Skia.Color('#D4AF37'));\ncanvas.drawRect({ x: width - (30 * scale), y: 60 * scale, width: 4 * scale, height: height - (120 * scale) }, paint);\n\n// 3. MASSIVE structural brand heading rotated vertically on the side for ultimate catalog look\ncanvas.save();\ncanvas.translate(width - (65 * scale), height - (80 * scale));\ncanvas.rotate(-90, 0, 0);\n\npaint.setColor(Skia.Color('white'));\nconst titleFont = oswald;\ntitleFont.setSize(72 * scale); // Prominent flagship-tier lettering\ncanvas.drawText(name.toUpperCase(), 0, 0, paint, titleFont);\ncanvas.restore();\n\n// 4. Clean baseline digital metadata track (Bottom Right)\nif (website) {\n  paint.setColor(Skia.Color('#A1A1AA'));\n  const webFont = regular;\n  webFont.setSize(22 * scale);\n  const textW = webFont.getTextWidth(website.toLowerCase());\n  drawTextLeft(canvas, website.toLowerCase(), width - textW - (90 * scale), height - (80 * scale), webFont, paint, width * 0.4);\n}\n\nreturn [\"name\", \"website\"];",
    "isPremium": false
  },
  {
    "id": "themeGlassmorphicStorefront",
    "name": "Glassmorphic Storefront",
    "type": "code",
    "requiredFields": [
      "name",
      "address",
      "phone"
    ],
    "code": "const { righteous, montserrat, icons } = await loadFonts();\nconst { name, address, phone, logo } = brandData;\nconst scale = getScale(width, height);\nconst paint = Skia.Paint();\n\n// 1. Premium floating glassmorphic container box anchored securely at the bottom\nconst cardW = width - (80 * scale);\nconst cardH = 170 * scale;\nconst cardX = 40 * scale;\nconst cardY = height - cardH - (50 * scale);\nconst radius = 24 * scale;\n\n// Intentionally soft, diffuse dropshadow for premium structural depth\npaint.setColor(Skia.Color('black'));\npaint.setAlphaf(0.15);\ncanvas.drawRRect(Skia.RRectXY({ x: cardX, y: cardY + (6 * scale), width: cardW, height: cardH }, radius, radius), paint);\n\n// Main Frosted Glass ultra-opaque white canvas backing\npaint.setColor(Skia.Color('white'));\npaint.setAlphaf(0.95);\ncanvas.drawRRect(Skia.RRectXY({ x: cardX, y: cardY, width: cardW, height: cardH }, radius, radius), paint);\n\nlet contentX = cardX + (40 * scale);\n\n// 2. High-precision circular mask ring clip for official retail logo asset\nif (logo) {\n  const lSize = 90 * scale;\n  const lY = cardY + (cardH - lSize) / 2;\n  \n  paint.setColor(Skia.Color('#1E293B')); // Premium deep slate border mask\n  paint.setAlphaf(1);\n  canvas.drawCircle(contentX + lSize / 2, lY + lSize / 2, lSize / 2 + 3 * scale, paint);\n\n  canvas.save();\n  const clip = Skia.Path.Make().addCircle(contentX + lSize / 2, lY + lSize / 2, lSize / 2);\n  canvas.clipPath(clip, 1, true);\n  \n  const { width: lW, height: lH } = getLogoDimensions(logo);\n  const s = Math.max(lSize / lW, lSize / lH);\n  drawLogo(canvas, logo, contentX - ((lW * s) - lSize) / 2, lY - ((lH * s) - lSize) / 2, lW * s, lH * s);\n  canvas.restore();\n  \n  contentX += lSize + (30 * scale);\n}\n\n// 3. Ultra-Loud Contemporary Display Typography (Righteous)\npaint.setColor(Skia.Color('#1E293B'));\npaint.setAlphaf(1);\nconst titleFont = righteous;\ntitleFont.setSize(46 * scale); // Prominent business naming scale\ndrawTextLeft(canvas, name, contentX, cardY + 60 * scale, titleFont, paint, cardW - (contentX - cardX) - (30 * scale));\n\n// 4. Micro-aligned store grid directory text data\npaint.setColor(Skia.Color('#475569'));\nconst directoryFont = montserrat;\ndirectoryFont.setSize(22 * scale);\n\nif (address) {\n  drawIcon(canvas, 'location', contentX, cardY + 90 * scale, 22 * scale, paint, icons);\n  drawTextLeft(canvas, address, contentX + (32 * scale), cardY + 110 * scale, directoryFont, paint, cardW - 150 * scale);\n}\n\ndrawIcon(canvas, 'call', contentX, cardY + 125 * scale, 22 * scale, paint, icons);\ndrawTextLeft(canvas, `ORDER: ${phone}`, contentX + (32 * scale), cardY + 145 * scale, directoryFont, paint, cardW - 150 * scale);\n\nreturn [\"name\", \"address\", \"phone\"];",
    "isPremium": false
  },
  {
    "id": "themeRetailMinimalOnyx",
    "name": "Minimal Onyx",
    "type": "code",
    "requiredFields": [
      "name",
      "email",
      "website"
    ],
    "code": "const { serif, regular } = await loadFonts();\nconst { name, email, website } = brandData;\nconst scale = getScale(width, height);\nconst paint = Skia.Paint();\n\n// 1. Sleek geometric studio label plate layout (Centered Top Header alignment)\nconst boxW = width - (100 * scale);\nconst boxH = 150 * scale;\nconst bx = 50 * scale;\nconst by = 50 * scale;\n\npaint.setColor(Skia.Color('rgba(18, 18, 20, 0.96)')); // Sharp premium solid onyx box block layer\ncanvas.drawRRect(Skia.RRectXY({ x: bx, y: by, width: boxW, height: boxH }, 14 * scale, 14 * scale), paint);\n\n// 2. Extra Prominent Luxurious Retail Brand Header (Serif execution style)\npaint.setColor(Skia.Color('white'));\nconst titleFont = serif;\ntitleFont.setSize(52 * scale); // Maximum premium editorial emphasis scaling\ndrawTextCentered(canvas, name.toUpperCase(), by + 65 * scale, titleFont, paint, width, boxW - (40 * scale));\n\n// Fine luxury architectural spacing track line divider\npaint.setColor(Skia.Color('rgba(255, 255, 255, 0.18)'));\ncanvas.drawRect({ x: bx + (80 * scale), y: by + 95 * scale, width: boxW - (160 * scale), height: 1.5 * scale }, paint);\n\n// 3. Elegant baseline communication footer data links\npaint.setColor(Skia.Color('#A1A1AA'));\nconst detailsFont = regular;\ndetailsFont.setSize(20 * scale);\n\nconst compositeString = `${email || ''}    •    ${website || ''}`;\nif (compositeString.trim() !== '•') {\n  drawTextCentered(canvas, compositeString.toLowerCase(), by + 130 * scale, detailsFont, paint, width, boxW - (40 * scale));\n}\n\nreturn [\"name\", \"email\", \"website\"];",
    "isPremium": false
  },
  {
    "id": "themeFlagshipBoutique",
    "name": "Flagship Boutique",
    "type": "code",
    "requiredFields": [
      "name",
      "socialHandle"
    ],
    "code": "const { bold, oswald, icons } = await loadFonts();\nconst { name, socialHandle } = brandData;\nconst scale = getScale(width, height);\nconst paint = Skia.Paint();\n\n// 1. Streamlined full-width solid baseline footer ribbon strip\nconst stripH = 115 * scale;\npaint.setColor(Skia.Color('rgba(15, 15, 18, 0.85)'));\ncanvas.drawRect({ x: 0, y: height - stripH, width, height: stripH }, paint);\n\n// 2. Giant prominent layout structural lettering block (Bold Type)\npaint.setColor(Skia.Color('white'));\nconst titleFont = bold;\ntitleFont.setSize(42 * scale); // Prominent lettering layout choice\ndrawTextLeft(canvas, name, 40 * scale, height - (stripH / 2) + (14 * scale), titleFont, paint, width * 0.5);\n\n// 3. Cyber-clean premium asset capsule badge anchor (Floating Right edge aligned)\nif (socialHandle) {\n  const badgeW = 280 * scale;\n  const badgeH = 54 * scale;\n  const badgeX = width - badgeW - (40 * scale);\n  const badgeY = height - (stripH / 2) - (badgeH / 2);\n  \n  // Emerald conversion clean green retail indicator highlight\n  paint.setColor(Skia.Color('#10B981'));\n  canvas.drawRRect(Skia.RRectXY({ x: badgeX, y: badgeY, width: badgeW, height: badgeH }, 27 * scale, 27 * scale), paint);\n  \n  paint.setColor(Skia.Color('white'));\n  const subFont = oswald;\n  subFont.setSize(24 * scale); // Crisp, bold tag sizing scaling\n  drawIcon(canvas, 'logo-instagram', badgeX + 22 * scale, badgeY + 13 * scale, 26 * scale, paint, icons);\n  drawTextCentered(canvas, socialHandle.toUpperCase(), badgeY + 36 * scale, subFont, paint, width, badgeW - 50 * scale);\n}\n\nreturn [\"name\", \"socialHandle\"];",
    "isPremium": false
  },
  {
    "id": "themeLuxuryShowcase",
    "name": "Luxury Showcase",
    "type": "code",
    "requiredFields": [
      "name",
      "slogan",
      "website"
    ],
    "code": "const { abrilFatface, lora, regular } = await loadFonts();\nconst { name, slogan, website, logo } = brandData;\nconst scale = getScale(width, height);\nconst paint = Skia.Paint();\n\n// 1. Sophisticated Asymmetrical Frame Layering (Top Left and Bottom Right)\npaint.setColor(Skia.Color('rgba(18, 18, 18, 0.88)'));\nconst panelW = width * 0.55;\nconst panelH = 220 * scale;\ncanvas.drawRect({ x: 0, y: height - panelH, width: panelW, height: panelH }, paint);\n\n// Premium Gold Accent Separator Strip\npaint.setColor(Skia.Color('#D4AF37')); // Champagne Gold\ncanvas.drawRect({ x: 0, y: height - panelH, width: panelW, height: 4 * scale }, paint);\n\n// 2. High-Impact Editorial Brand Header (Abril Fatface)\npaint.setColor(Skia.Color('white'));\nconst titleFont = abrilFatface;\ntitleFont.setSize(54 * scale); // Oversized luxury retail focus\ndrawTextLeft(canvas, name.toUpperCase(), 40 * scale, height - panelH + 75 * scale, titleFont, paint, panelW - (60 * scale));\n\n// 3. Spaced Serif Tagline (Lora)\npaint.setColor(Skia.Color('#D4AF37'));\nconst subtitleFont = lora;\nsubtitleFont.setSize(22 * scale);\nif (slogan) {\n  drawTextLeft(canvas, slogan.toUpperCase(), 40 * scale, height - panelH + 120 * scale, subtitleFont, paint, panelW - (60 * scale));\n}\n\n// 4. Floating Anchor Web Address Link (Bottom Right with dynamic backdrop Protection)\nif (website) {\n  const webFont = regular;\n  webFont.setSize(20 * scale);\n  const tw = webFont.getTextWidth(website.toLowerCase());\n  \n  // Subtle background mask patch for web anchor readability over raw photo content\n  paint.setColor(Skia.Color('rgba(0, 0, 0, 0.5)'));\n  canvas.drawRRect(Skia.RRectXY({ x: width - tw - (60 * scale), y: height - (65 * scale), width: tw + (40 * scale), height: 40 * scale }, 12 * scale, 12 * scale), paint);\n  \n  paint.setColor(Skia.Color('white'));\n  canvas.drawText(website.toLowerCase(), width - tw - (40 * scale), height - (38 * scale), paint, webFont);\n}\n\nreturn [\"name\", \"slogan\", \"website\"];",
    "isPremium": false
  },
  {
    "id": "themeEditorialCommerce",
    "name": "Editorial Commerce",
    "type": "code",
    "requiredFields": [
      "name",
      "slogan",
      "logoUri"
    ],
    "code": "const { merriweather, montserrat, icons } = await loadFonts();\nconst { name, slogan, logo, website, email } = brandData;\nconst scale = getScale(width, height);\nconst paint = Skia.Paint();\n\n// 1. Elegant Framed Bottom Glass-Draw Box Layout\nconst panelW = width - (70 * scale);\nconst panelH = 190 * scale;\nconst px = 35 * scale;\nconst py = height - panelH - (40 * scale);\n\n// Soft diffuse premium card dynamic backdrop shadow\npaint.setColor(Skia.Color('black'));\npaint.setAlphaf(0.2);\ncanvas.drawRRect(Skia.RRectXY({ x: px, y: py + (5 * scale), width: panelW, height: panelH }, 20 * scale, 20 * scale), paint);\n\n// Main High-End Frosted White Body\npaint.setColor(Skia.Color('white'));\npaint.setAlphaf(0.97);\ncanvas.drawRRect(Skia.RRectXY({ x: px, y: py, width: panelW, height: panelH }, 20 * scale, 20 * scale), paint);\n\nlet contentX = px + (40 * scale);\n\n// 2. Premium Circle Logo Seal Alignment\nif (logo) {\n  const lSize = 100 * scale;\n  const lY = py + (panelH - lSize) / 2;\n  paint.setColor(Skia.Color('#0F172A')); // Deep corporate slate edge\n  paint.setAlphaf(1);\n  canvas.drawCircle(contentX + lSize / 2, lY + lSize / 2, lSize / 2 + 3 * scale, paint);\n\n  canvas.save();\n  const clip = Skia.Path.Make().addCircle(contentX + lSize / 2, lY + lSize / 2, lSize / 2);\n  canvas.clipPath(clip, 1, true);\n  const { width: lW, height: lH } = getLogoDimensions(logo);\n  const s = Math.max(lSize / lW, lSize / lH);\n  drawLogo(canvas, logo, contentX - ((lW * s) - lSize) / 2, lY - ((lH * s) - lSize) / 2, lW * s, lH * s);\n  canvas.restore();\n  contentX += lSize + (25 * scale);\n}\n\n// 3. Luxurious Editorial Headline Typography (Merriweather)\npaint.setColor(Skia.Color('#0F172A'));\npaint.setAlphaf(1);\nconst headlineFont = merriweather;\nheadlineFont.setSize(44 * scale); // Large premium store title presence\ndrawTextLeft(canvas, name, contentX, py + 65 * scale, headlineFont, paint, panelW - (contentX - px) - (30 * scale));\n\n// 4. Modern Structured Subtext Arrays (Montserrat)\npaint.setColor(Skia.Color('#D4AF37')); // Premium Brand Gold\nconst itemFont = montserrat;\nitemFont.setSize(20 * scale);\nif (slogan) {\n  drawTextLeft(canvas, slogan.toUpperCase().split('').join(' '), contentX, py + 105 * scale, itemFont, paint, panelW - (contentX - px) - (30 * scale));\n}\n\n// Secondary Connection detail\nconst connection = website || email || \"\";\nif (connection) {\n  paint.setColor(Skia.Color('#64748B'));\n  drawIcon(canvas, 'globe', contentX, py + 132 * scale, 20 * scale, paint, icons);\n  drawTextLeft(canvas, connection.toLowerCase(), contentX + (28 * scale), py + 152 * scale, itemFont, paint, panelW - (contentX - px) - (50 * scale));\n}\n\nreturn [\"name\", \"slogan\", \"logoUri\"];",
    "isPremium": false,
    "isNew": true
  },
  {
    "id": "themeBoldStreetwearRetail",
    "name": "Bold Streetwear Retail",
    "type": "code",
    "requiredFields": [
      "name",
      "socialHandle"
    ],
    "code": "const { righteous, oswald } = await loadFonts();\nconst { name, socialHandle } = brandData;\nconst scale = getScale(width, height);\nconst paint = Skia.Paint();\n\n// 1. Raw Minimalist Industrial Box Configuration (Top Center Header Alignment)\nconst containerW = width - (80 * scale);\nconst containerH = 130 * scale;\nconst cx = 40 * scale;\nconst cy = 50 * scale;\n\npaint.setColor(Skia.Color('#0D0D11')); // Deep Carbon Box Layer Shield\ncanvas.drawRect({ x: cx, y: cy, width: containerW, height: containerH }, paint);\n\n// Vibrant Cyber-Neon Dynamic Corner Stripe Trim Accent\npaint.setColor(Skia.Color('#38BDF8')); // Premium Light Cyber Blue Accent\ncanvas.drawRect({ x: cx, y: cy, width: 8 * scale, height: containerH }, paint);\n\n// 2. Extra Loud Geometric Display Typography (Righteous Focus Layout)\npaint.setColor(Skia.Color('white'));\nconst labelFont = righteous;\nlabelFont.setSize(48 * scale); // Prominent bold product watermark focus\ndrawTextLeft(canvas, name, cx + (35 * scale), cy + 62 * scale, labelFont, paint, containerW - (60 * scale));\n\n// 3. Sub-Asset Retail Category Tagline tracker line\nif (socialHandle) {\n  paint.setColor(Skia.Color('#38BDF8'));\n  const subFont = oswald;\n  subFont.setSize(22 * scale);\n  drawTextLeft(canvas, `OFFICIAL CATALOGUE // @${socialHandle.replace('@','').toUpperCase()}`, cx + (35 * scale), cy + 104 * scale, subFont, paint, containerW - (60 * scale));\n}\n\nreturn [\"name\", \"socialHandle\"];",
    "isPremium": false
  },
  {
    "id": "themePremiumConversionPill",
    "name": "Premium Conversion Pill",
    "type": "code",
    "requiredFields": [
      "name",
      "phone",
      "socialHandle"
    ],
    "code": "const { bold, regular, icons } = await loadFonts();\nconst { name, phone, socialHandle } = brandData;\nconst scale = getScale(width, height);\nconst paint = Skia.Paint();\n\n// 1. Clean Structured Baseline Protection Ribbon Grid\nconst stripH = 110 * scale;\npaint.setColor(Skia.Color('rgba(15, 15, 20, 0.84)'));\ncanvas.drawRect({ x: 0, y: height - stripH, width, height: stripH }, paint);\n\n// 2. High-Impact Structural Brand Headline text block\npaint.setColor(Skia.Color('white'));\nconst titleFont = bold;\ntitleFont.setSize(40 * scale); // Loud crisp alignment presence\ndrawTextLeft(canvas, name, 40 * scale, height - (stripH / 2) + (13 * scale), titleFont, paint, width * 0.45);\n\n// 3. Conversion Pill Badge Component Block (Floating Right Alignment Edge)\nif (socialHandle || phone) {\n  const activeVal = socialHandle ? `@${socialHandle.replace('@','')}` : phone;\n  const subFont = regular;\n  subFont.setSize(22 * scale);\n  const textW = subFont.getTextWidth(activeVal);\n  \n  const pillW = Math.max(260 * scale, textW + (80 * scale));\n  const pillH = 54 * scale;\n  const pillX = width - pillW - (40 * scale);\n  const pillY = height - (stripH / 2) - (pillH / 2);\n  \n  // Emerald Corporate Conversion Highlight Color\n  paint.setColor(Skia.Color('#10B981'));\n  canvas.drawRRect(Skia.RRectXY({ x: pillX, y: pillY, width: pillW, height: pillH }, 27 * scale, 27 * scale), paint);\n  \n  paint.setColor(Skia.Color('white'));\n  drawIcon(canvas, socialHandle ? 'logo-instagram' : 'call', pillX + 22 * scale, pillY + 14 * scale, 24 * scale, paint, icons);\n  drawTextCentered(canvas, activeVal, pillY + 35 * scale, subFont, paint, width, pillW - 40 * scale);\n}\n\nreturn [\"name\", \"phone\", \"socialHandle\"];",
    "isPremium": false
  },
  {
    "id": "themeMinimalistFlagshipAura",
    "name": "Flagship Aura",
    "type": "code",
    "requiredFields": [
      "name",
      "address",
      "website"
    ],
    "code": "const { serif, regular, icons } = await loadFonts();\nconst { name, address, website } = brandData;\nconst scale = getScale(width, height);\nconst paint = Skia.Paint();\n\n// 1. Ultra-Minimalist Geometric Float Card Layering (Bottom Anchored Safe Box)\nconst blockW = width - (80 * scale);\nconst blockH = 160 * scale;\nconst bx = 40 * scale;\nconst by = height - blockH - (50 * scale);\n\npaint.setColor(Skia.Color('rgba(255, 255, 255, 0.94)')); // Luxe high-alpha pristine crystal backdrop layer\ncanvas.drawRRect(Skia.RRectXY({ x: bx, y: by, width: blockW, height: blockH }, 16 * scale, 16 * scale), paint);\n\n// Distinctive Fine Border Geometry Lining\npaint.setStyle(1);\npaint.setStrokeWidth(1.5 * scale);\npaint.setColor(Skia.Color('rgba(0, 0, 0, 0.08)'));\ncanvas.drawRRect(Skia.RRectXY({ x: bx, y: by, width: blockW, height: blockH }, 16 * scale, 16 * scale), paint);\npaint.setStyle(0);\n\n// 2. Massive Elegant Flagship Store Serif Identifier\npaint.setColor(Skia.Color('#111111'));\nconst styleFont = serif;\nstyleFont.setSize(50 * scale); // Scaled very large for flagship luxury store emphasis\ndrawTextCentered(canvas, name.toUpperCase(), by + 65 * scale, styleFont, paint, width, blockW - (40 * scale));\n\n// Fine luxury linear separating layout line rule\npaint.setColor(Skia.Color('rgba(0, 0, 0, 0.1)'));\ncanvas.drawRect({ x: bx + (80 * scale), y: by + 92 * scale, width: blockW - (160 * scale), height: 1.5 * scale }, paint);\n\n// 3. Clean Muted Digital Meta Directories Row\npaint.setColor(Skia.Color('#5A5A66'));\nconst detailFont = regular;\ndetailFont.setSize(20 * scale);\n\nconst textString = website ? website.toLowerCase() : (address ? address : \"\");\nif (textString) {\n  drawTextCentered(canvas, textString, by + 128 * scale, detailFont, paint, width, blockW - (40 * scale));\n}\n\nreturn [\"name\", \"address\", \"website\"];",
    "isPremium": false
  }
]